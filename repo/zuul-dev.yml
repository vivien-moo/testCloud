eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/
server:
  port: 8081
spring:
  application:
    name: zuul
  profiles:
    active: dev
zuul:
  routes:
    api:
      path: /api/**
      serviceId: config-server
